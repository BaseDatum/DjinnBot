/**
 * ModelSelector — re-exports ProviderModelSelector for backward compatibility.
 * All new code should use ProviderModelSelector directly.
 */
export { ProviderModelSelector as ModelSelector } from './ProviderModelSelector';
