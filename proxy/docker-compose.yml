# DjinnBot Reverse Proxy — Traefik
# This file is generated by `djinn setup`. Do not edit manually.
# Run `djinn setup` to configure HTTP-only or SSL mode.
#
# If you need to regenerate this file, run: djinn setup

services:
  traefik:
    image: traefik:v3
    container_name: djinnbot-traefik
    restart: unless-stopped
    command:
      - --entrypoints.web.address=:80
      - --providers.docker=true
      - --providers.docker.network=djinnbot-proxy
      - --providers.docker.exposedbydefault=false
      - --log.level=WARN
      - --accesslog=false
    ports:
      - "80:80"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - djinnbot-proxy
    healthcheck:
      test: ["CMD", "traefik", "healthcheck"]
      interval: 30s
      timeout: 5s
      retries: 3

networks:
  djinnbot-proxy:
    external: true
